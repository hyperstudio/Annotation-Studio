<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title><%= yield(:page_title) %> :: Annotation Studio :: <%= ENV["ENVIRONMENT_TAG"] %></title>
    <%# include_gon(:namespace => 'serverExports') %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
    <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag "application", :media => "all" %>

    <meta name="robots" content="noindex">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="google-site-verification" content="<%= ENV["GOOGLE_VERIFICATION_CODE"] %>" />

    <link href="/favicon.ico" rel="shortcut icon" />
    <%= javascript_include_tag "application" %>
</head>
<body data-spy="scroll" data-target="#chapters">
    <div class="container">

    <div class="navbar navbar-default navbar-fixed-top" role="navigation">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="offcanvas" data-target="#myNavmenu" data-canvas="body">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="#">Annotation Studio</a>
        </div>
        <div class="navbar-collapse collapse">
          <a class="hidden-xs logo" href="/"><img src="/assets/hs-logo-trans-horiz.png"></a>
          <ul class="nav navbar-nav" id="accountlinks">

                            <%= render 'devise/menu/registration_items' %>
                            <%= render 'devise/menu/login_items' %>

          </ul>
          <ul class="nav navbar-nav">
              <% if params[:controller] == 'documents' && params[:action] == 'show' %>
              <li><a data-toggle="modal" href="#help">Help</a></li>
              <% end %>
          </ul>
        </div><!--/.nav-collapse -->
      </div>
    </div>

        <%= render partial: "shared/flash_messages", flash: flash %>
        <noscript>
            <div class="alert alert-danger">For Annotation Studio to work, it is necessary to enable JavaScript. Here are <a href="http://www.enable-javascript.com/" target="_blank"> instructions how to enable JavaScript in your web browser</a>.</div>
        </noscript>

        <%= yield %>

        <footer>
            <p>&copy; MIT <%= Time.now.year %></p>
        </footer>
    </div><!-- /container -->

    <script type="text/javascript">

        var _gaq = _gaq || [];
        _gaq.push(['_setAccount', '<%= ENV["GOOGLE_ANALYTICS_CODE"] %>']);
        _gaq.push(['_trackPageview']);

        (function() {
            var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;
            ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
            var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s);
        })();

    </script>
</body>
</html>
