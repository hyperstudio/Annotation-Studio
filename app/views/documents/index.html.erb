<div class="span12">
	<h1>Documents</h1>
	<table class="table table-striped">
	  <thead>
	    <tr>
	      <th>ID</th>
	      <th>Title</th>
	      <th>Created at</th>
	      <th>Actions</th>
	    </tr>
	  </thead>
	  <tbody>    
	    <% @documents.each do |document| %>
	      <tr>
	        <td><%= document.id %></td>
	        <td><%= link_to document.title, document_path(document.id) %></td>
	        <td><%= document.created_at %></td>
	        <td><%= document.updated_at %></td>
	        <td>
	          <%= link_to 'Edit', edit_document_path(document.id), :class => 'btn btn-mini' %>
	          <%= link_to 'Destroy', document_path(document.id), :method => :delete, :confirm => 'Are you sure?', :class => 'btn btn-mini btn-danger' %>
	        </td>
	      </tr>
	    <% end %>
	  </tbody>
	</table>
	<%= link_to 'New', new_document_path, :class => 'btn btn-primary' %>
</div><!--/span12 -->
