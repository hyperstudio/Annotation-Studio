<% if user_signed_in? %>
  	<li><%= link_to("Dashboard", "/dashboard", :id => "user-account-name", :data => { :toggle => "popover", :content => ""}, :title => current_user.fullname) %></li>
	<script type="text/javascript" charset="utf-8">
		$('#user-account-name').popover({
			'html': 'true',
			'animation': 'true',
			'placement': 'bottom',
			'trigger': 'hover',
			'content': "<ul><li><%= current_user.rep_subgroup_list %></li></ul>",
		}).data('popover').tip().css('z-index', 1040);
	</script>
<% else %>
	<li>
  		<%= link_to('Register', new_user_registration_path)  %>
	</li>
<% end %>